import React from "react";
import "../Estilos/Display.css";

const Display = ({input})=> (
<div className="input">
    
    
    {input}

</div>
);

export default Display;

//const numeroRedondeado = math.format(input, { notation: 'fixed', precision: 13 });